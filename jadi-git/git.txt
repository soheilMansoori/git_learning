open folder in ubuntu => mkdir
open file in ubuntu => touch
show all hidden files and folders in ubuntu => ls -ltrha


git status

git add file-name

git add .
git add -A
git add '*.js'

git git commit -m 'message'

git log 


git diff HEAD
git diff --staged

git reset file-name
git checkout --file-name

git branch
git branch branch-name

git merge branch-name

git barnch -d barnch-name
git branch -D branch-name

git clone githubPage url

git push origin master

git pull origin master

git remote add origin my-repo in github


git tag 

git tag -a tag-name -m 'message'

git tag -a tag-name -m 'message'  commit-hash

git push origin tag-name

git push origin --tags

git checkout tag-name
